# 基础设施验收

状态：集成问题已关闭，临时 vault 全量验收通过，已按用户的条件式授权切换结构化术语编辑源并启用首批术语参考消费。外部正式 vault 写入、合并和发版不在本次执行范围。

## 数据与权限

已登记 157 个术语概念、314 个名称及 51 条来源；原 62 个实体保持原记录值及字节块。全部原行、原有身份、名称去向及当前依据见[数据登记](2026-09-06-term-data-migration.md)。六项定义许可、三项项目定义及两项项目英文形式由精确 L3 决定控制，布局同样要求 L3 精确授权。

程序不硬编码本批概念编号或定义全文。普通 record 批准不能代替项目 scope 或定义来源许可，历史名称不扩大当前准用名称；相同字符串在其他合法词表对象中的效力保留。

## 工程检查

在集成边界执行核心与 Obsidian 全量回归，发现的问题均保留真实失败及对应修复，不以删除正式记录、放宽权限或增加兼容层处理。

核心首次运行 285 项，两个失败分别来自测试夹具复制了真实术语批准，以及旧测试把任意 origin 字段或字符串一概禁止。夹具改为自身建立术语数据和决定，仅复用所需六份来源上下文；删除没有独有证据、且被严格来源 schema 覆盖的全仓字符串扫描，其余迁移账本检查保留。相关命令测试和四项账本测试均复测通过。

应用首次运行 112 项，日期读取不一致导致 6 个失败和 16 个错误。核心 v2 接受的规范 ISO 日期字符串，原应用却只接受 YAML 原生 date；现按同一日期合同归一为内部 date，并拒绝 datetime、非法日期和非规范写法。受影响的来源、术语、快照、初始化与绑定模块共 59 项通过，另补验 CLI 11 项通过；没有重跑已经通过且不受影响的模块。

独立复审发现完整布局接受 L1 批准，已改为 L3 并由 18 项布局与生成窄测及独立复核确认。权限、日期和索引的最终增量结论见[工程复审](2026-09-06-term-complete-engineering-review.md)。

## 实际入口

`check-sources --root .` 返回零来源问题；`check-topics` 返回零问题，现有 700 个概念和 24 个数组保持原样。新增来源使实体数变为 113；维护指标提醒不等于数据校验失败，不自动开启义务或改变状态。

实际 `term-data index` 暴露六项许可决定只有自身节点、没有概念目标边的问题。现直接从同次捕获的有效决定读取明确 patch.identity，生成 declared 目标边，不遍历旧值、不赋予批准效力。维护模块九项测试通过，真实索引已可反查六个许可对象。

Markdown 链接检查原先把 YAML 批准载荷中按 glossary 定位的字符串误当作决定正文链接；现只跳过合法 frontmatter，保持正文与行号检查，实际结果为全部链接有效。完整布局中的显示链接仍在最终生成页接受正文检查。

## 临时验收

实现检查点为 d237d68。按已批准条件式执行建立隔离 worktree，42ec468 投影临时状态，5f694651fb2bd45aa2a712e8267ac3ca3ad24985 包含完整生成页。临时投影不用于外部正式库，主工作树在验收完成前仍保持 proposed publication。

应用保护规则拒绝在仓库非 output 目录初始化 vault，因此实际测试 vault 使用系统临时目录，设计输入和验收材料仍位于 build/term-complete-acceptance。没有放松目录保护或扩大正式写集。

全量新库初始化包含 1067 个受管理文件。另以 71b2b3b 的原六词表快照初始化旧库，加入一条合法 draft、旧引用、个人配置及个人文件，再由完整快照执行刷新。结果新增 208 个参考文件，包含 51 个来源页与 157 个术语页；21 个非参考保护文件的前后哈希完全一致。

升级后的 1055 个 kb 参考文件与全量新库逐文件哈希相同。旧引用验证通过；在测试 draft 中增加内容单元术语页的引用后，再次验证仍为零问题。项目依据明确显示为项目决定，已退出的历史名称不进入当前 aliases。没有将测试 draft 或临时投影当作正式内容使用证据。

完整术语生成的 build/check 比较无漂移，生成页链接有效。六词表预览的真实读取与 HTML 构建返回 error 为 null，未增加第七词表或修改其只读职责。已按实际概念绑定同步现有说明，见[术语引用同步](2026-09-06-term-reference-sync.md)；没有为文案同步重跑行为测试。

## 编辑源切换

上述条件满足后，主工作树将 term-complete-publication 记录为 accepted，写入真实 active 状态及切换历史，再从正式输入重建 docs/glossary.md。概念、定义和现行形式由 terms.yaml 维护；编排、说明、符号和明确历史名称由 layout 维护；模型标签维持原有主题、载体及语言采纳输入。

只读声明已按这些所有权校正，不能让读者把模型标签或展示说明误写进术语数据。当前术语参考消费不等于内容消费者、义务、委托、持久正式索引、TBX 或回流启用。外部正式库和 master 均未写入。
