# 预览验收

日期：2026-09-05。分支：`feat/vocab-preview`。范围见[实施计划](../plans/2026-09-05-vocab-preview.md)。

## 应用结果

现有词表浏览页面与采集逻辑已纳入 `apps/vocab-preview/`，通过 uv 启动本地只读服务。默认读取本仓库 `data/vocab/`，页面定期检查内容变化并自动重载；没有生成需要另行刷新的静态快照。

## 验收证据

- `uv run --offline python -m unittest discover -s apps/vocab-preview/tests`：7 项通过。覆盖工作区数据更新、原始 YAML 同步、错误后保留与恢复、结构错误、语言依据嵌套形状、重复集合键、HTTP 只读边界和内嵌文本转义。
- 当前六份词表读取成功，页面 JavaScript 语法检查通过；正式词表与根 README 的 Git 差异为空。
- 浏览器打开正式预览，确认词表列表、人工智能搜索、详情中的语言依据与来源、全库关系图正常显示。
- 浏览器使用独立临时词表：名称由“实时预览测试甲”保存为“实时预览测试乙”后自动显示；写入不完整 YAML 后保留旧页面并显示文件错误；修正后自动显示“实时预览恢复成功”。测试没有编辑正式词表，临时服务和页面已关闭。
- 独立审查发现的语言依据形状与重复集合键问题均已复现、修复并通过定向复审，无剩余发现。
- uv 锁文件、包内页面资源、现行文档链接与 Git 差异检查通过。

## 维护边界

页面仍为只读工作区表示，不承担正式校验或人工采纳。自动重载会重置搜索、筛选和展开状态，精细保留后置。原本地预览目录保留，旧生成文件、重复脚本与研究缓存未搬入源码。核心库和 Obsidian 业务实现未改动，因此没有重复运行其全量回归。
