# 终端访问验收

日期：2026-09-05。分支：`codex/obsidian-p0`；基线：`3d2ea99`。范围依据为用户批准的 CLI 使用规则、新库受管理代理入口和[终端访问决定](../../docs/decisions/obsidian-agent-entry.md)。

## 应用结果

新库初始化生成根 `AGENTS.md` 并以 `rule` 类型纳入 manifest。原有用户、参考、报告与配置职责不变。旧库没有条目时不强制新增，未登记的同名用户文件不会被接管；词表刷新保持已有规则及其权属。

原生 CLI 的定位、超时和结果检查由调用方承担，本次没有增加包装器。可复用规则不包含本机 vault ID、个人目录或新的写入权限。

## 验收证据

| 失败与后果 | 执行证据 |
|---|---|
| 根规则漂移后仍能创建内容，导致未获准的代理规则参与写入 | 新测试先复现内容建立未阻断，修复后拒绝写入且全库字节不变 |
| 刷新拒绝新规则或接管旧库用户文件，破坏新旧库兼容与权属 | 新测试先复现根路径不受支持；修复后保留已登记规则，漂移阻断，旧用户文件原样保留且不进入清单 |
| 共用校验改动破坏其他内容与刷新流程 | `uv run --offline python -m unittest discover -s apps/obsidian/tests -t apps/obsidian`：97 项通过 |
| 新库虽有规则却无法建立或读取有效内容 | 临时干净设计仓库初始化成功，清单覆盖 858 个受管理文件；建立一条临时 draft 后内容校验无问题；临时库已清理 |
| 同名库定位错误或假成功被当成可用结果 | 本机 CLI 只读验证：名称定位及参数后置不能可靠选中正式库；使用精确 ID 并将参数前置后返回正式绝对路径；缺失文件以退出码 0 返回错误文本，调用方必须检查结果 |
| 文档入口失效，无法定位现行合同 | `uv run --offline python scripts/check-links.py` 通过；`git diff --check` 通过 |

正式库 `/Users/xiu/Documents/kb-vault` 的已观察内容为《了解 Obsidian》一条 draft。原生 CLI 标题搜索、正文上下文搜索与内容 Base 查询返回同一 UUID 文件；应用校验结果为 1 条有效记录、0 个问题。这些是读取和结构证据，不是正文外部事实核实或 GUI 交互验收。

## 独立审查

审查只核对代理权限、文件权属和刷新写集，没有重复运行机械检查。发现生成规则未明确 uv 执行目录，可能使外部 vault 中的写后校验无法启动；已补充从工具所在设计仓库根执行，并通过定向复核关闭。没有剩余 Critical 或 Important 发现。

## 交付边界

`build/obsidian-agent-entry/AGENTS.md`、`AGENTS.diff` 和 `manifest-entry.json` 为忽略的本地审阅产物；正式库目前没有根规则，因此正文差异为新增文件。清单条目仅供评估，不是可绕过迁移审阅的补丁或安装命令。

正式库、旧库注册信息、来源与术语数据均未修改；未合并到主分支，未推送或发版。多份真实材料的录入、界面交互、受治理原生 CLI 写入和既有库规则安装仍未完成，不把本次子阶段完成称为完整 P0 验收。
