# 来源收尾提案

状态：待采纳。本提案是完整核对后的集中处置，不能把此前“采纳上述建议”的回复套用到本次新增值。当前工程提交为 `f4087c5`，master 未合并。精确值、旧值及出处见[结构化清单](2026-09-06-source-completion-proposals.json)。

## 核对结果

[全量引用清单](2026-09-06-source-reference-proposals.json)覆盖全部 757 条映射：3 条已采纳、726 条形成具体建议、28 条待处置。692 条外部派生中 690 条有具体建议，2 条随历史代码问题处置；24 个外部分组都有身份依据，但不是成员完整或顺序一致的证明。

47 条非 self 归属已有完整逐值依据建议，原主题挂接不变；另有 OpenClaw 的一项挂接需要明确取舍。旧 14 条 self 归属已在上批按项目判断保留，不转成外部事实。

材料不足与判断门槛分别处理：来源限定的忠实派生可以支持概念对应推断，不能仅因没有 scope 就一律阻断；真实范围冲突、版本不明或缺少材料仍须保留。正式版 GB/T 镜像已经核到，早先版本不明的转载不再用来证明 2009 正式版。

## 批次建议

建议一次采纳以下完整范围，而不是逐条重复申请开工。

| 范围 | 具体动作与边界 |
|---|---|
| 已核引用 | 采纳清单中 726 条映射、690 条派生及 24 个分组的具体值；3 条此前已采纳关系不重复批准。分组只证明外部组身份，不顺带批准其他层级或完整性 |
| 逐值依据 | 采纳 47 条 typed subjects 依据；外部材料与内部关联推断分别记录，不改变原 subjects |
| 维护字段 | 采纳[逐实体维护清单](2026-09-06-source-maintenance-proposals.json)中 31 条记录的 93 个明确值：全面复核日期为空，按现有档级配置周期；只配置可用性和重定向观察；替代对象为空只表示未登记。旧日期和观察地址进入历史，绝不回填今天或宣称已复核 |
| 用途资格 | 补采纳 SWEBOK 的 mapping／structure、ASVS 的 structure、ATLAS 的 structure；其余已采纳和 proposed 角色保持，各具体关系仍按清单 |
| 版本与地址 | ASVS 明确选用 5.0.0；SWEBOK 保留 4.0，DITA 保留已读 2015 的 1.3 原版；7 个未能确认登记版本的来源使用 null 并保存旧值。补齐清单中的访问入口；不把地址登记当作已核可用性。DITA 三个版本地址来自原版的规范元数据 |

这批维护配置是具体的人为选择，不是从 schema 允许直接推出的默认批准；不启用 live 探测、正式义务或新的消费者。

## 范围修正

结构化清单给出以下完整 before／after，均保留概念 ID 与 label。

- CS2023 五项：计算复杂度、形式语义、计算机系统概览、着色与高级渲染、机器人平台。分别纠正计算难度与可计算性的混淆、两种语义机制、应用与操作系统服务、对焦和景深、ROS 示例与必要条件。
- SWEBOK 维护：补明交付前规划、可维护性及交接准备，不只描述交付后维护。
- LLM06：明确过多功能、权限、自主性三个维度，不只指权限；不新增中文 designation，不改原标签或冒充重新核过官方中文。

建议同时采纳这七项修正后的来源限定映射。原语言采纳与 original.scope 快照保留；实施以新决定记录更正，不回写旧采纳日的事实。

## 个别处置

| 对象 | 推荐动作 |
|---|---|
| 人才学 | 保留原 ID、名称、父项与历史 6305520 对应，依据中明确该外部条目已经删除；不宣称现行条目，不同一化为新管理学或社会学对象 |
| 教育心理学 | 按正式版的主表、参见和新旧代码对照，将外部指向改为 19070；本库 ID 不变，原教育父项解释为内部主题关联，不能称原样主分类复制 |
| Pydantic | 软件身份保持；把“本库指 v2”从 scope 移为 version: "2"，只表示项目选用主要版本，再按软件身份保留 Wikidata 对应 |
| OpenClaw | subjects 只保留 artificial-intelligence，去掉缺乏开发工具用途依据的 tools-and-environments；不改变实体或 active 状态，也不声称它不能编程 |
| ASVS 密码学 | 将对应改为 closeMatch，保留应用安全要求与通用密码学之间的范围区别；不修改 CS2023 主派生和对应 |

上述动作均是本次明确的取舍，不能由校验器自行推断。清单给出完整字段值与材料定位。

## 数组边界

本批 24 个既有来源数组保留当前成员和顺序，作为本库的明确选择；external_group 只指向已核的外部组，不再把这些记录说成逐序完整复制。CS2023 的 SPD-SEP 缺项以及部分数组的顺序差异继续保留审查。本批不新增概念，不修改父项，不把同级文档标题自动当成新单元。

这是对本次既有数组的明确处置，不自动放宽未来新借入的规则。若选择恢复逐序完整复制，则需要另外处理缺失单元和顺序，而不能同时宣称现状完整不变。

## 隔离范围

建议将最后 17 条尚无合格对应依据的关系留在审查记录：15 条 LOM 映射，以及 Advice 和 reference → TechArticle。原关系、来源、旧值与结论完整保存在本次集中采纳决定的隔离清单和 Git 历史；现有对象不删除、不搬走，标签、状态和其他关系不因此变化。

这 17 条不进入正式 match，不显示为已核关系；不增加旧格式兼容读取，不以人的同意替代缺少的正式版或语义证据。LOM 正式 2002 正文在有限查找后仍未取得；Advice 与 TechArticle 则是对应依据不足，不是页面不存在。

该处置需要明确采纳，因为它替代原方案“必需未核关系一律阻断整个切换”的边界。严格新格式仍只承载有依据的正式引用；缺失事实保持可审查，而不是伪造 basis 或删除记录制造通过。

## 实施写集

采纳后整批完成：决定与迁移输入、必要的精确引用纠错和隔离门禁、七项范围更正、六份新格式词表、模型与 target 的对应说明，以及全量生成、索引和临时导出验收。预计约 2–4 小时；若出现新的实质问题如实报告，不把估计当作完成保证。

恢复使用 Git。不会新增通用交付或补偿回滚框架；保留原语言采纳历史，正式 vault 不在写集。全部验证完成前不合并 master；本提案也不把资料审阅或候选生成称作正式切换。
