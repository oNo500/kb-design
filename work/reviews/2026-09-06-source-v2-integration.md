# 来源整合记录

状态：隔离分支实施中，尚未完成正式切换。工作区为 `codex/source-v2-prep`，原基线为 `633f684`，工程及首批采纳已保存为 `f4087c5`。本文记录当前交付与未解决条件，不以测试数量或候选目录存在代替完整迁移。

## 已采纳范围

[字段合同](../../docs/decisions/source-v2-field-contract.md)、[原四项字段](../../docs/decisions/source-field-values.md)及[本次批次](../../docs/decisions/source-data-batch.md)均可由机器读取。最新批次含 149 项具体 patch，另有一项模型控制：外部状态可缺省，但明确表示未核实；已有值继续要求依据和精确采纳。

具体 patch 包含字段与依据、用途资格、14 项 subjects 项目判断、8 项本地建立、2 项载体数组及 3 条已核关系。patch 数不是独立事实数。迁移输入已接入三条关系及两个数组；其余源记录不能为了补齐生成输入而伪造 reviewed。

## 工程结果

- 核心、schema、索引和生成器采用严格 v2；旧冻结材料只作审计。
- 项目 status 与外部 source_status 分开；保留一般实体字段、逐值 subjects 依据及独立项目判断。
- 角色批准核对对象、角色与状态；structure 与 group 均须获准 mapping，structure 仍受档级和版本限制。
- 迁移器拒绝用空结果删除结构化引用、项目判断或隔离对象；既有身份、成员与关系受到保留约束。
- Obsidian 与预览使用同一份捕获输入校验，明确显示来源状态与版本的缺口，不把隔离对象显示为有效来源。
- 模型、维护和 target 文档同步新合同；没有恢复兼容层、补偿回滚或提交白名单。

## 验证结果

检查针对权限绕过、身份与关系损失、生成不确定性及导出语义损失。核心整合门禁 75 项通过；随后针对状态缺省变更的 16 项核心行为测试通过。最新 Obsidian 定向测试 8 项通过，预览 8 项通过。独立语义审查发现的角色资格绕过和空迁移删除问题均已修复并复核。

这些结果证明受测行为，不是使用当前正式数据的全量发布验收。旧正式数据的严格检查仍失败；没有宣称完整回归、正式导出或切换通过。schema 生成结果与文件一致，Git 差异格式检查通过。

## 候选范围

`build/source-v2-integration/adopted-candidate/` 保存已采纳字段的诊断候选，不是正式数据或发布输出。它应用 129 项字段变更，来源角色中 20 项 approved、27 项 proposed；保留其他尚未采纳的旧值以暴露缺口，不将其当作合法新值。

与基线逐项比较的 846 条记录保留 ID 与顺序、名称、类别、项目状态、subjects、父项、成员、scope、form／vendor／creator、added 和语言依据；77 条记录含已采纳变更。正式六份词表的哈希和冻结审计目录没有变化。

候选当前在 schema 阶段报告 282 项问题，主要涉及尚未采纳的旧引用、逐值依据、复核与观察字段。校验在该阶段停止，282 不是全部剩余语义问题，也不能用它估算完成比例。详细结果和保留检查位于候选目录的 JSON 中。

## 全量核对

本轮已将 757 条映射逐项归入[引用提案](2026-09-06-source-reference-proposals.json)：3 条已采纳、726 条有具体建议、28 条仍需处置。692 条外部派生中 690 条有建议，2 条涉及历史代码；24 个分组身份都有建议。该清单按当前记录和旧目标逐项对应，拒绝漏项、错对象及非法新形状；没有把其他来源的审查覆盖主派生结论。

GB/T 正式版镜像的封面、附录与相关表页已经核实，原先高校 HTML 的版本疑点作为历史观察保留。第二号修改单的官方附件已读，不影响本批代码。SWEBOK 找到可识别的 4.0 镜像转录，但只使用实际可读范围；第 18 章对应明确依赖目录而非未读正文。LOM 最终出版正文在有限查找后仍未取得。

## 收尾范围

[集中收尾提案](2026-09-06-source-completion-proposals.md)给出明确的下一次数据采纳，不把已有“继续实施”授权冒充新增事实采纳。它包括逐值依据、维护字段、七项范围修正、历史代码与个别对象处置，并提出将最终 17 条未获合格依据的关系仅保留在审查记录；对应对象不删除、不搬走。

这 17 条的审查处置会改变原“所有必需未核关系均阻断整个切换”的范围，需要人明确采纳。未采纳前，全部新建议仍是候选，正式数据、生成输入和 schema 门禁不会因此被悄悄放宽。

## 切换条件

收到明确采纳后，才把新增具体值及隔离处置接入生成器，并完成六份严格数据、生成确定性、索引和临时导出验证。旧语言采纳快照保留，不回填核对日期，不建立旧格式兼容或通用补偿回滚。

本轮补查依据的是此前重要来源只读核验授权，未扩展译名、不登录、不做批量资料下载或 OCR、不使用 Computer Use。正式 vault 未写入，master 未合并；主 checkout 的 AGENTS.md 和 writing.md 用户修改保持原样。
