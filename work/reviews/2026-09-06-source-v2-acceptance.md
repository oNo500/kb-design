# 来源迁移验收

状态：来源 v2 数据、实现、验收与本地合并已完成。完整实现快照为 `9903027`，该干净提交已通过实际 init／validate／report 链路；后续交付记录不改变其实现或数据。实施依据为[来源收尾决定](../../docs/decisions/source-completion.md)；本记录区别迁移验收与未核事实，不宣称所有外部关系都已证实，也不激活术语或真实内容消费者。

## 数据结果

六份词表均采用 v2。700 个概念、24 个主题数组、62 个实体和 31 个来源登记保持稳定身份；846 条记录的顺序、名称、项目状态、类别、档级、父项、成员、添加日期及原语言依据逐项核对。范围与归属变化仅限决定明确批准的八项 scope 和 OpenClaw subjects。

740 条映射进入正式新格式；17 条原关系完整保存在决定的隔离清单，原对象及其其他关系保持。692 条外部派生和 24 个外部分组都有明确定位与依据；分组表示本库的既有选择，不宣称逐序完整复制。17 条隔离关系分别是 15 条 LOM 映射、Advice 和 reference → TechArticle。

原实体元数据完整进入真实迁移事件的 history.before，旧核对日期不冒充全面复核。语言采纳文件及 original.scope 快照未改写。历史人才学的已删除限定、教育心理学 19070 指向、Pydantic 的所选主要版本和缺省外部状态均在输出中可见。

## 验收证据

| 目标失败与后果 | 实际结果 |
|---|---|
| 字段形状、来源资格或精确授权失效 | check-sources 对完整数据及旧基线校验为零问题；check-topics 为零问题 |
| 重建覆盖更正或输出不确定 | 再生成的 topics.yaml 与当前文件逐字节相同 |
| 原身份、关系或日期丢失 | 846 条记录保留检查通过；每个隔离或纠正关系均对应精确原索引与决定；17 条旧关系未丢失 |
| 历史语言语境被覆写或借用决定 | 精确 scope 与 scope.correction 联合授权测试通过；原语言采纳快照保持 |
| 索引漏掉有效引用或混入审查事实 | 正式引用边数为 740／692／24，索引共 4051 项；索引同时覆盖角色、实际历史决定及本地隔离决定，不遍历历史 before／after 或未核 patch 值 |
| 新格式破坏应用链路 | Obsidian 全量 107 项通过；预览 8 项通过；核心全量 225 项通过 |
| 导出静默丢字段或把未核关系当作有效对应 | 实际临时参考导出生成 848 份内容文件及 manifest；关键条目及全部 17 条隔离关系的呈现检查通过；干净 Git 快照的新库初始化、校验、报告链路通过，空库结果不作为真实内容使用证据 |

检查发现并修复了归档候选被过度拒绝、报告模块仍读旧 source、日期序列化不符合原 YAML 日期合同，以及历史决定未入反向索引的问题。测试中的旧阶段假设也已改为独立空输入与明确新合同，没有把错误期待改成宽松通过。

独立审查未发现 Important 或 Critical 问题，重点覆盖隔离、改指、范围授权、语言历史、归档候选和捕获快照读取。诊断产物位于 Git 忽略的 build/source-v2-close/，不作为另一份正式编辑源。

## 保留边界

17 条隔离关系仍未取得完整对应依据，按已采纳决定留待以后核对；它们不再被算作本批格式迁移的未完成步骤。来源复核与观察配置不代表 live 探测或正式义务已经运行。持久正式索引、术语数据、正式消费者、回流、发版和正式 vault 刷新均不由本次验收启用。

本次没有操作 ~/Documents/kb-vault/，没有新增兼容层、提交白名单或补偿回滚。恢复依靠 Git；主 checkout 的用户写作规则改动保持原样。

## 本地交付

已将通过验收的分支快进合并到 master，未推送或发版。主 checkout 的 AGENTS.md 原有用户补充通过临时保存后完整恢复，writing.md 原有修改字节保持一致；没有提交这两处用户改动。保留干净的来源工作树供需要干净设计快照的工具使用，未清理仍有历史证据的工作树或缓存。
