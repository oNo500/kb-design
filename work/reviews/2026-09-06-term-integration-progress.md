# 术语集成进展

本记录只证明已授权的工程、取证与提案准备；不是术语数据采纳、正式切换或基础设施最终验收。执行分支为 `codex/term-infrastructure`，基线为 `7778e6e`。主分支后续提交 `088610a` 的写作要求已阅读，本分支不覆盖该提交。

## 当前结果

结构与取证范围分别由[术语基础范围](../../docs/decisions/term-infrastructure-scope.md)和[术语依据范围](../../docs/decisions/term-evidence-scope.md)采纳。统一准入、生成、引用索引、维护命令和 Obsidian 可选术语参考读取已实现。具体值见[术语数据提案](2026-09-06-term-data-candidate.md)和[来源候选](2026-09-06-term-source-candidate.md)。

143 个原定义行中，56 行提出 65 个概念，84 行保留现行登记待证据，3 行保留为说明。6 条出处缩写、14 条文献说明和 231 条模型译名均有去向；模型译名对应既有 256 个对象，不新增术语概念。原行中未采纳的残余名称或符号另有逐项保留方案。审计记录不承担生产编辑权。

## 校验依据

准入与历史检查防止无具体批准的值进入生成、稳定身份搬动或旧历史改写。生成、维护和应用读取使用同一核心检查；Git 前态来自最近一次术语数据变更的父版本，后续纯文档提交不能隐藏改写。显式前态文件只能增加比较，不能覆盖 Git 前态。输入字节模式只消费已捕获数据。

核心风险阶段回归命令为 `uv run python -m unittest discover -s packages/kb-core/tests`，277 项通过，耗时 36.867 秒。之后的术语表阅读结构修正由渲染窄测覆盖：概念名称可读，详细内容不混入章节目录，独立术语页保留原结构。随后修复带页码描述的 URL 定位链接，9 项渲染窄测通过。

`uv run kb-core check-sources --root .` 返回 0 个来源问题；`uv run kb-core check-topics` 返回 0 个问题；`uv run python scripts/check-links.py` 返回全部链接有效。来源义务仍未激活，指标提醒不等于准入失败。首次来源命令漏传 `--root`，已修正后重新执行。

应用风险阶段回归使用 `uv run python -m unittest discover -s apps/obsidian/tests -t apps/obsidian`。初次调用遗漏 `-t` 导致相对导入失败，不能作为应用失败或通过证据；修正命令后 118 项全部通过，耗时 226.286 秒。

具体候选通过术语 schema，且在实际缺少采纳决定时被正式准入拒绝。现存问题仅为具体决定、模型采纳和初次状态授权缺失；未用伪造 accepted 决定消除这些问题。ISO 15489 的处置定义已实际重读免费样章 §3.8，补齐真实核对日期。

## 展示核对

`build/term-review/candidate-glossary.md` 是带未采纳声明的临时渲染预览。它展示概念、语言形式、定义、出处、模型依据和现行登记引述，不能替代生产生成入口。已检查受控词表、实体、属性、处置等代表性内容；概念的外部依据与中文模型语言依据分开展示。

完整生成仍须接入拟议保留登记编辑源；该路径和效力须随具体数据一次采纳。当前没有覆盖 `docs/glossary.md`，也没有创建正式 `terms.yaml` 或切换状态。

## 正式库预检

原生 Obsidian CLI 的 vault 列表及路径查询确认目标为 `/Users/xiu/Documents/kb-vault`。只读核对 manifest 后，当前设计版本为 `cf80d1d34c4aeef6e590f4f6d81c00643acc3eb3`，859 个受管理文件无缺失，其中 `kb/topics/software-verification-and-validation.md` 的字节与 manifest 不同；`content/` 有 21 个 Markdown 文件。

该本地修改已记录，尚未决定其处置，不得在未来刷新时静默覆盖。本轮没有修改 vault、配置或内容。该预检不是新版本 refresh dry-run；后者须等数据切换、干净提交及临时 vault 验收完成。

## 待采纳事项

一次集中采纳应覆盖具体来源与术语值、残余形式、保留登记编辑源与效力、章节布局、生成与诊断读取、精确发布状态，以及获准后的迁移和临时 vault 统一验收。具体写集及控制值已放在数据提案，不另留一次未知控制字段批准。

取得该采纳后才实施保留源衔接、正式数据迁移、完整生成和统一验收，预计仍需 1–2 小时。完成前不合并 master。正式 vault 写入及发版不包含在本次采纳中。
