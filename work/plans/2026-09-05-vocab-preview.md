# 预览迁入计划

状态：用户已批准，实施与验收完成。分支 `feat/vocab-preview`，使用当前 checkout。实施范围按会话收窄为迁入源码、uv 启动、工作区实时更新和必要验证，预计 15–30 分钟。

## 执行范围

- 现有 `template.html` 及数据采集逻辑进入 `apps/vocab-preview/`，旧目录保留；不复制静态快照、重复检查产物或研究缓存。
- 加入 uv workspace，命令 `uv run kb-vocab-preview`，默认地址 `http://127.0.0.1:8765`，支持数据根与端口参数。
- 读取六份工作区词表，通过内容变化检测自动重载页面；无效数据保留上一次有效结果并提示错误。
- 同步架构、应用设计、使用说明和工作路线；不修改根 README、正式词表、审计或旧 vault。

## 验收条件

| 失败与后果 | 证据 |
|---|---|
| 页面仍显示旧数据 | 临时词表修改后的 HTTP 数据及浏览器自动重载 |
| 编辑错误造成空白页或无法恢复 | 非法 YAML 与错误结构出现提示，修复后恢复 |
| 只读预览写回数据或暴露其他文件 | 请求前后临时目录内容一致，非公开路由与写入请求被拒绝 |
| 数据文本变为可执行页面代码 | 含脚本结束标记的文本按 JSON 数据安全嵌入 |

仅运行相关行为测试、当前数据读取和浏览器检查，不重复执行未改动的核心库与 Obsidian 全量回归。搜索、筛选和展开状态的精细保留不在本次范围。

验收记录见[预览验收](../reviews/2026-09-05-vocab-preview.md)。
