# 治理

治理是对管理的管理：谁有权决定什么、按什么政策、变更怎么准入、结果怎么被监督。它不直接操作词表和内容——那是[维护](maintenance.md)的职责——而是规定维护由谁做、按什么规则进行、怎样接受审计。理论见[治理](../concepts/governance.md)。

本库的治理主体是一个人，管理层是一个 AI。ISO 37000 对治理主体与管理层的区分在这里形成授权边界：AI 可以直接做什么、必须先提案什么、哪些事项只有人能决定。首批边界见[决策权的首批边界](decisions/decision-rights-defaults.md)。schema、索引、观察、义务接口、生成器、诊断或校验通过，只证明相应机械能力，不产生事实结论，也不扩大任何一方的决策权。

## 对象范围

治理对象按效力分开。正式数据、现行设计、`Application Profile`、导出 artifact contract、已经实现但未激活的基础、迁移审计和草案不能互相替代；target 文件或生成目录存在也不证明真实内容消费者已经启用。

### 现行记录

| 对象 | 在哪 | 生命周期／验收 |
|---|---|---|
| 主题词表、命名实体词表、文档类型词表 | `vocab/topics.yaml`、`vocab/entities.yaml`、`vocab/types.yaml` | 状态机见各词表设计；校验见[维护](maintenance.md) |
| 来源名称规范表 | `vocab/sources.yaml` | [来源名称规范表](sources-registry.md)；当前仍是旧正式形状 |
| 词表的生成输入 | `vocab/build/` | 修改生成输入等同修改数据，适用相应词表规则 |
| 变更记录与指标快照 | `vocab/CHANGELOG.md`、`vocab/signals.yaml` | 只追加；见[维护](maintenance.md)的审计追踪 |
| 来源 | `vocab/entities.yaml` 中 `kind` 为 standard／publication 的实体 | 现行 `tier` 与复核见[维护](maintenance.md) |
| 设计文档 | `design/*.md` | 本文“质量与验收” |
| 决定记录 | `design/decisions/` | 只追加；推翻时新建记录并标明替代关系 |
| `Application Profile` 与导出 artifact contract | `design/targets/` | [内容模型](content-model.md)的上层职责和五个操作问题；[应用约束与表示分层](decisions/application-profile-boundary.md) |
| 概念文 | `concepts/` | [CONVENTIONS](../concepts/CONVENTIONS.md) 的硬规则 |
| designation 登记 | `concepts/glossary.md` | 现行可编辑登记；准入规则见本文“准入对象” |
| 文献笔记 | `sources/` | 来源已经核对；阅读范围写明 |
| 脚本 | `scripts/` | 本文“质量与验收”；脚本中的现行阈值常量与阈值表一致 |
| 摘要 | `AGENTS.md`、`README.md` | 随被摘要的文档同步 |
| 知识库内容 | 在别处 | [内容模型](content-model.md)与各应用映射 |
| 治理规则本身 | 本文 | 本文“治理评审” |

正式 `vocab/entities.yaml`、`vocab/sources.yaml` 和 `vocab/topics.yaml` 继续决定当前数据形状。语言依据按[已采纳决定](decisions/structured-label-basis.md)结构化并补齐本批缺失中文；其他旧引用不因此切换为后置共享结构，既有标签、id、状态、`tier`、用途和关系不变。

现行 [Obsidian 映射](targets/obsidian.md)按应用无关模型、`Application Profile` 语义选择和导出 artifact contract 分工。`kb-design` 单向导出六份正式词表，独立 `kb-obsidian` 应用已实现新 vault 初始化、内容建立、只读校验和派生报告。生成文件可以在 Obsidian 中编辑，但修改不回流、不取得项目效力；本地持久 vault 当前没有实际用户内容，消费者没有正式激活，也没有可审计查询日志或回流接口。

### 能力边界

以下基础已经实现，但没有接管正式数据。

| 范围 | 已实现能力 | 未取得的效力 |
|---|---|---|
| 来源 | 六份 schema、共享来源模型、离线校验、反向索引、固定夹具探测、迁移预演和复核义务接口 | 没有正式来源 v2 数据、正式义务、正式索引、真实联网观察、具体角色批准或正式切换 |
| 术语 | 三层候选模式、状态转换校验、确定性生成器、正文诊断、维护索引和复核义务接口 | 没有正式术语数据、义务、委托、消费者、切换状态或编辑权转移 |
| Obsidian | 六份正式词表的单向参考导出、新 vault 初始化、UUIDv4 `draft` 建立、只读内容校验、派生报告、项目 manifest 和安全发布 | 没有实际用户内容、正式消费者、查询日志、回流、JCS、BagIt、reproducible build 或 durability 符合性与保证 |

来源探测的 `--live` 明确禁用，实际 JSONL 输出与探测 schema 尚未闭合。固定夹具只能证明只读行为和信号逻辑，不能确认真实来源状态。术语生成器和维护接口没有正式输入；测试夹具中的状态与消费者也不是仓库现状。

仓库当前不存在正式 `vocab/source-obligations.yaml`、`vocab/terms.yaml`、`vocab/term-obligations.yaml`、来源正式索引或术语正式切换状态。`concepts/glossary.md` 仍是现行可编辑登记；本次同步不创建上述正式对象。

### 迁移边界

`vocab/migrations/source-v1/` 的六份账本保存来源旧身份、旧用途、旧 `basis`、旧 `source`、旧 `match`、旧 `origin` 的库存、分类和阻断。账本的推荐值、`proposed` 或处置不批准真实来源事实，不修改正式数据；既有预演只能作为已保存的历史结果，不能宣称当前 HEAD 可按原冻结输入重放。

`vocab/migrations/term-v1/terms.tsv` 的 348 条记录保存冻结审查结论和消费者去向。`audit-only`、`retain-owner`、`retain-pending-l3`、旧 `keep`、`defer` 或 `remove` 都不构成 designation 准入、概念对应、术语候选、删除许可或正式状态。

迁移账本只作审计。它们不是正式词表、义务、委托、消费者、决定或切换状态。

### 草案边界

`design/drafts/` 中的文档各自写明触发条件。来源治理和术语治理的部分前置机器契约已经另经决定实现，不表示草案整体生效。草案中的角色、状态、周期、委托、正式数据和切换安排都不能在人工生效决定前写入正式对象。

草案生效仍走变更控制。阅读草案、实现 schema 或工具、生成 ignored 材料，均不取得正式规则效力。

## 政策

全库硬规则统一列在下表。每条政策都标明依据；`AGENTS.md` 是本节和[写作规则](writing.md)的摘要。

| 政策 | 内容 | 依据 | 详见 |
|---|---|---|---|
| 来源优先 | 已有规范和事实标准是设计的来源和依据；外部事实须核对原文。本阶段仅用已有离线资料，未核实事实如实标明；经授权的模型知识译名单独登记 | Z39.19 §11.1.5 要求采用表示形式前对照权威来源核验 | [写作规则](writing.md) |
| 先概念后设计 | 采用任何方法前先有概念文和方法登记，再导出规则；推不出的规则不加 | Aristotle 第一原理；Nygard ADR，见[第一原理与设计理由](../concepts/first-principles.md) | [方法登记](principles.md) |
| 零自定 | 项目采用的 designation 必须按准入阶梯取得依据；AI 不得主动形成、选定或借状态试用无依据的 designation。普通叙述、来源转录、路径、代码和值不因首次出现纳入禁令 | ISO 25964-1 §2.11 概念先于表示形式；ISO 704；Z39.19 §11.1.5 | 本文“准入对象” |
| 实体类别取外部类 | 实体的 `kind` 取 Wikidata 的类，不自定 | 零自定的实例 | [命名实体词表](entities.md) |
| 断言有依据 | 人判断的字段记 `basis`；`basis` 含 `self` 的现行记录不得为 `active`。designation 的准入依据、概念对应依据和具体断言依据分门，任何一项都不能替代另一项 | Z39.19 §11.1.4 的来源记录；W3C PROV-O | [维护](maintenance.md) |
| 整篇重写 | 改文章不打补丁；改动涉及一节以上时整节或整篇重写，并逐项核对旧版去向 | 本库的过程规则，见[写作规范](../concepts/writing-conventions.md) | `AGENTS.md` |
| 先用数据试 | 规则定稿前用真实数据跑一遍 | 本库的过程规则，来自已发生的失败 | 本文“审计” |
| 验证投入 | 审查和测试按目标失败及后果配置；没有独有风险证据、已被更强门禁覆盖或只证明机械事实的检查不设计、不执行 | ISO 19011:2026 §4.7、§4.8；[决定记录](decisions/verification-effort.md) | 本文“验证投入” |
| 范围决定顶层 | 顶层概念由范围声明决定；顶层之下按知识体系全借，借到有稳定编号的最深一层 | ISO 25964-1 §13.1；Z39.19 §11.1.3、§11.1.7、§11.1.8 | [层级结构](hierarchy.md)规则 3–5 |
| 原样复制 | 复制的结构原样保留，不拆不改；本地分析另建数组；同一视角只取一个来源 | [决定记录](decisions/borrow-and-analyze.md) | [层级结构](hierarchy.md)规则 6、13 |
| 结构来源资格 | 现行正式旧数据中，可作结构来源的实体必须具有正式 `structure` 用途，并为 `de-jure`，或为有版本号的 `de-facto`；`vendor` 只作映射。后置严格接口另要求经决定批准的角色，但尚未激活 | Z39.19 §5.3.5.2 组织依据；来源分级 | [来源名称规范表](sources-registry.md) |
| 映射来源登记 | 现行 `source`、`match.source`、语言依据的 `references[].source` 和其他紧凑 `basis` 引用的来源必须登记在正式 `vocab/sources.yaml`。后置共享 `basis.entity`、`source.registry` 和 `match.registry` 已实现但未切换 | ISO 25964-2 §23 名称规范表 | [来源名称规范表](sources-registry.md) |
| 不删 | `deprecated` 概念记录和内容单元保留；旧表示形式按既有检索、替代关系或历史追踪需要随概念记录保留。只有本地 `candidate` 概念记录可以按维护条件取得删除资格，实际删除仍服从决策权 | Z39.19 §11.3.2；ISO 15489-1 §3.8 处置决定 | [内容模型](content-model.md)、[维护](maintenance.md) |
| id 不变 | 现行 id 一经引用不改；改名只改 `label`，旧形式按检索和历史需要保留并建立替代引导。候选术语接口的独立身份格式不适用于现行词表，也不表示正式身份已经分配 | Z39.19 §11.3.1.2 的替代与引用处理 | [内容模型](content-model.md) |
| 与工具无关 | 与应用无关层不提工具字段和格式；当前 `vocab/` 正式数据是源，各应用从它导出，不反向编辑；映射不得改动字段定义。未激活生成能力不转移编辑权 | [决定记录](decisions/form-independence.md) | [内容模型](content-model.md) |
| 决定审计追踪 | 规则的结构性变更走决定记录，只追加不修改 | Nygard ADR | [决定记录](decisions/) |
| 写作规则 | 标题、标点、项目命名和文档类型按写作规则 | ISO 24495-1 四原则 | [写作规则](writing.md) |

政策本身的变更走变更控制。观察、索引、义务、迁移分类、报告命中和工具通过只能提供复核材料；它们不能自动修改正式值、批准具体来源或 designation，也不能提升权限。

## 决策权

决策权分三级：AI 可直接做、AI 提案后由人决定、只有人能决定。边界按改动的可逆性和是否改变规则划分。

| 级 | 改动 | 例 |
|---|---|---|
| **AI 直接做** | 不改规则、可逆、有脚本或规则可验 | 数据采集与核对；按已定规则生成或校验数据；修正错字、标点和标题违规；替换同一来源的坏链接；同步索引；在 CHANGELOG 当前版节内追加；按既定小节清单写已批准文档；新实体以 `candidate` 录入 `entities.yaml`；核到 `basis` 文字且校验通过后批准（`candidate` → `active`） |
| **AI 提案，人定** | 改规则、改结构，或没有规则可依 | 新设计文档或概念文；修改任何规则或阈值，包括脚本阈值常量；文件拆并、改名、删除；词表结构变更；新方法登记；designation 准入；`scope`、`broader`、`subjects` 的判断；删除 `candidate` 对象时列清单；换来源或改用 archive 快照；概念文修订导致规则变化 |
| **只有人定** | 改变范围或不可逆 | 范围声明；零自定例外；决定记录的采纳与推翻；删除任何非 `candidate` 对象；发版；草案生效；来源改档 |

提案须说明改什么、为什么、依据是什么、影响哪些文件。人回复后才能修改文件；AI 不得把第二级事项当作第一级事项。

模式或工具可以要求决定引用，却不能形成或采纳决定。观察到来源变化、建立反向索引、计算或重开义务、生成报告、通过 schema 或测试，都不改变上表。具体来源状态、地址、角色、替代、实际派生、概念映射、术语准入、概念对应、优先形式、委托和迁移去向仍按其内容落入第二级或第三级。

每条提交说明以 `[L1]`、`[L2]` 或 `[L3]` 标注本次改动的最高级别。人抽查 `[L1]` 提交是否确属第一级；发生越权或欠权后，本表修订一次并记决定。

## 变更控制

| 变更 | 流程 | 记录 |
|---|---|---|
| 规则变更 | AI 提案 → 人定 → 改文档 → 同步受影响文档与摘要 → 链接检查 | 默认视为结构性，记决定；人可降为局部，只记提交说明 |
| 政策变更 | 同规则变更，必须有决定记录 | 决定记录 |
| 方法准入 | 概念文 → 方法登记 → 导出规则 | [方法登记](principles.md) |
| designation 准入 | 本文“准入对象”的阶梯 | `concepts/glossary.md` 的出处记录与查无记录 |
| 概念文修订 | 修订 → 检查方法登记中由它导出的规则 → 受影响规则走规则变更 | 提交说明 |
| 草案生效 | 触发条件出现 → 人确认 → 草案移出 `drafts/` → 内容并入相应设计 | 决定记录 |
| 词表结构变更 | 提案 → 人定 → 修改 `vocab/build/` 输入并重新生成，或直接改数据 → 校验通过 | CHANGELOG |
| 来源改档 | 人定 → 修改实体 `tier` → 复核受影响断言 | 实体 `history` |
| 词表发版 | 维护触发 → 人定 → 新建版本块与 CHANGELOG 版节 | [词表版本](versioning.md) |
| 新建 target | 核对概念文 → 核对[方法登记](principles.md) → 引用适用的已采纳决定 → 写 `Application Profile` 上层职责与五个操作问题 → 分开导出 artifact contract → 人定 | `design/targets/` 与决定记录 |
| target 语义变更 | 核对应用无关模型和决定 → 提案语义选择、约束或 loss 的变化 → 人定 → 同步 artifact contract 和入口 | target 文件与提交说明；结构性变更记决定 |
| 导出物化变更 | 在既定语义选择内修改 bytes、顺序、quoting、newlines、file set、manifest、校验或 publication → 定向验证 | target 文件、实现和提交说明；越过语义边界时转 target 语义变更 |
| 消费者启用 | 具名消费者、内容字段或检索输入、运行契约与验收证据齐备 → 人定 | 决定记录；静态 target 文件和生成目录不能代替 |
| 推翻决定记录 | 新建决定 → 旧决定标为被替代 | 决定记录 |
| 摘要同步 | 被摘要文档改动后同步 `AGENTS.md`、README | 同一提交 |

来源正式迁移、术语正式激活、编辑权转移、委托、消费者启用和切换状态，都必须等待其阶段由人开放并取得所需决定。本次已有基础没有建立这些对象，也不提供完整 candidate、handoff、payload、原子应用或补偿回滚。

## 准入对象

项目拟采用的每个 designation 都必须在 `concepts/glossary.md` 登记准入根据。通常按下列来源阶梯检索，查到即止，写明在哪一级取得依据。既有概念的译名另有经人开放的模型知识例外，按本节“译名”的第 5 级记录；模型判断不能登记为外部出处。

| 级 | 来源 | 例 |
|---|---|---|
| 1 | de-jure：国际与国家标准、W3C Recommendation、RFC | ISO 25964、GB/T 13745、SKOS |
| 2 | de-facto：知识体系、行业组织规范 | CS2023、SWEBOK、ASVS、Diátaxis |
| 3 | vendor：厂商文档 | Anthropic 文档、Obsidian 文档 |
| 4 | archival：论文、书、带日期的博文 | Hogan 2021、Bowker & Star 1999 |
| 5 | 其他：行业领袖的定义，或多个独立来源一致使用的业界共识 | Nygard 的 ADR |

一般 designation 按上述五级均查无时，由人制定词典条目：人给出定义、判据句和适用范围，登记为本库词典，`basis: self`。除“译名”第 5 级明确开放的范围外，AI 只有检索权限：查到后引用并登记，查不到如实报告，不得主动形成或选定无依据的 designation，也不能借 `candidate`、`defer`、草案、占位、示例、schema 状态或迁移去向试用。该译名例外不授权 AI 新建概念、项目类别或划分特征。普通叙述、来源转录、文件路径、代码和值不因字符串首次出现自动成为项目采用的名称。

查无记录必须可审计。提案逐级列明检索过的来源和结果；空白不算检索过。一个 designation 的形式依据、它所表示的概念以及记录中具体字段值或关系的依据须分别核对，任何一个状态门禁都不能替代另一个。

`scripts/check-terms.py` 是已经实现的候选识别器。它从 Git 中取得动态 Markdown 清单，检查标题、加粗内容和中文引号，排除代码围栏、行内代码、链接目标和路径；再与现行 `concepts/glossary.md` 及三份现行词表中的已登记写法比较。默认输出是带精确位置的 `report-only` 人工复核材料，命中不构成项目名称、概念、关系或违规，也不阻断提交。可选状态或人工裁定输入目前不改变这个效力。

348 条术语迁移账本只继承既有审查位置、动作和去向。它不建立候选术语记录，不重准入现行 designation，也不改变 `concepts/glossary.md` 的编辑权。候选术语三层模式只有接口效力；未来正式数据只有在另行生效后才可能接管登记。

划分特征草案中的属性名同样适用本节。将来需要采用时，由人按词典制定流程决定；划分特征的零自定例外尚未开放，不受译名例外扩及。

### 译名

中英对照只在 `concepts/glossary.md` 一处登记；词表的 `label` 与正文必须使用已登记形式。译名按下列阶梯处理，取得适用结果后停止。[译名依据扩展](decisions/industry-translation-basis.md)固定外部用法与名称变更边界；[译名检索来源](decisions/translation-reference-resources.md)细化固定术语库的使用；[模型知识译名](decisions/model-knowledge-translation.md)新增模型既有知识的使用级别。前四级按具体条目的编审与公布依据判断，不按网站整体赋级；第 5 级记录模型判断，不能宣称已经核实外部行业用法。

| 级 | 采用根据 |
|---|---|
| 1 | 等同采用国际标准的 GB 译名，以及全国科学技术名词审定委员会正式审定公布的名词；须确认具体标准或审定成果、版本、学科与条目对应 |
| 2 | 专业审译机构正式公布且编审身份可核的学术名词，或两个独立学科权威教材、工具书一致采用的译名；地区用法单独记录，不以繁简转换替代术语判断 |
| 3 | Wikidata 中文标签 |
| 4 | 多个独立来源一致使用、且与目标概念对应已经核实的业界惯用译名 |
| 5 | 在给定可用资料范围内，前四级未取得可采用结果时，允许模型依据训练形成的既有专业与语言知识提出行业术语或惯用译法；明确标为模型判断，外部用法未核实 |
| 6 | 模型也无法给出与目标概念对应明确的行业用法，或存在未解决的实质含义冲突时，保留原名并解释；登记中的 `zh` 留空，解释写入 `explanation` |

第 4 级承接“准入对象”中多个独立来源一致使用的业界共识，但中文形式与目标概念的对应仍须单独核实。使用前记录前三个级别的检索结果，并满足以下条件。

- 至少两个独立来源在相关领域的实际技术内容中一致使用该译名；同一内容的转载、镜像或相互转引不构成独立证据。不能只凭搜索结果数量或单次出现认定业界惯例。
- 通过明确的中英对照、定义或足以判断的上下文，确认用例所指与目标概念一致。只有中文相同、字面可译或领域相近，不足以建立对应。
- 在现行登记及其审查说明中保留每个来源的链接、用例位置或原文、可得的发布日期、核对日期、适用领域、概念对应理由和采纳结论，并注明依据属于业界用法；来源本身仍按现行准入规则登记。
- 证据不足时，不宣称第 4 级通过，可转入第 5 级另行处理。实质含义分歧或概念对应不清不能靠出现次数或模型判断消除。连接词等表面差异须结合上下文判断，不因不逐字相同就否定一致用法。

第 5 级是人明确开放的译名例外。AI 可以根据既有知识提出并选择其判断为行业使用的表达，不要求为该级补出两个独立外部用例。适用对象限于已有英文或中文原名、定义或范围可读的既有概念，包括课程领域和知识单元标题；不据此新增概念、重写定义、改变层级、映射或稳定 id。已知与原意冲突的用法，以及模型也不能确定含义的名称，进入第 6 级，不靠拼接术语凑齐标签。

本阶段仅使用当前已有离线资料和模型既有知识。已有离线核查未形成可用结果即可转入第 5 级，不要求联网或追加下载；未查的在线来源仍记为未查，不能写成已穷尽前四级。模型不能逐条追溯训练样本，因此不得编造训练资料、链接、引文或用例，也不能把一次模型输出或多次模型一致输出当作行业共识证明。

第 5 级在现行登记或审查记录中写明英文原名、所选译名、原概念范围、模型判断说明、实际可得的模型标识、生成日期及已查离线资料，并明确注明“模型既有知识，外部用法未核实”。不能由模型自行给出处或把模型标识登记为来源实体。译名采纳仍按现行决策权或用户明确授权的批次执行；规则开放本身不自动采纳待补标签；本次已批准的有限批次见[批次授权](decisions/structured-label-basis.md#批次授权)，后续新增不沿用该授权。

已有合格的前级译名不被第 5 级覆盖。“有译名但不达意”的判断只有人能做，AI 不得以此为由跳过前级。反方向采用相同阶梯与记录要求。第 6 级只给解释，不给名称。

现行词表的 `basis.zh`／`basis.en` 按[语言依据](topics.md#语言依据)分别保存外部 `level` 与 `references`、第 5 级 `model`、第 6 级未采用原因和未重新分级的 `legacy`。模型记录包含实际模型、日期、逐条判断和授权定位；它不是外部来源。第 6 级只适用于已作出未采用判断的空标签，旧 `none` 不能自动归入该级。旧 `self` 及其不得为 `active` 的规则保留，不机械转为第 5 级。

来源原名不要求重新翻译；本次只按[语言依据结构](decisions/structured-label-basis.md)的明确映射，把既有来源依据归级并保留原定位。这不宣称新做外部核验，不把来源整体类别用于新增译名准入。新增第 1—4 级依据仍按具体条目的适用条件核实；第 4 级的多个来源 ID 通过校验，不等于真实独立性和概念对应已经证明。

业界用法、权威译名和来源均可能变化，模型知识也可能过时或判断错误。有新用例、分歧或更高阶依据时，按[维护](maintenance.md#来源的复核)及对应的表示形式审核流程复核；模型重新生成本身不触发标签替换。概念未变而译名变化时，更新首选标签，按检索和历史追踪需要保留旧称与变更记录，稳定 id 不变；旧称不再常用不等于概念应转为 `deprecated`。概念替代、拆分或合并另走概念变更流程。模型知识等级与概念生命周期分开，不因第 5 级使用而自动改变概念状态。

`scripts/lookup-labels.py` 仍只提供第 3 级 Wikidata 查询，兼容旧字符串和结构化历史依据，不查询模型记录。它只接受对方语言标签与本库名称完全一致、描述与相应概念相符的条目；中文优先简体标签，繁体经 OpenCC 转简并注明。结果交人工判断，不能因脚本命中直接采纳；本阶段不运行联网查询。

旧人工决定保存在 `vocab/build/label-decisions.json`，采纳项记录 Q 号与标签，否决项记录理由。生成器先保留这一既有处理，再读取独立的 `label-adoptions.json` 应用已授权补全；后者不改写旧否决。未采纳的历史空白保留 `legacy: none`，不等于已穷尽检索。生成与校验共同检查结构化依据、现行来源和采纳一致性，不直接编辑 `topics.yaml`，也不把模型或新外部证据塞入 Q 号字段。

2026-08-23 首次追溯共 692 条自译，采纳 72 条、否决 13 条、不译 607 条；这是扩展前的历史结果，不是第 4 级业界用法的检索结论。不译的概念在 `scope` 中按来源原文给出中文解释，输入为 `vocab/build/scope-zh.json`；来源自带官方译文时直接使用，依据记 `source`。`check-topics.py` 报告现行 `self`；`check-terms.py` 只报告候选字符串和上下文，不裁定中英对应关系。两者都不证明多来源证据或业界共识已经核实。

### 译名检索

一般检索优先通过固定术语库或可取得的版本资料成批查找，再核对条目定义与领域。本阶段按用户限定，只查当前已有离线副本；以下在线入口仅说明资料出处，不授权联网操作或新增下载。离线缺口可按译名第 5 级使用模型既有知识，采纳仍按适用权限办理。各入口的用途如下。

| 入口 | 检索用途 | 证据边界 |
|---|---|---|
| 术语在线与《计算机科学技术名词》等审定成果 | 优先查规范科技名词、英文对应和定义 | 平台还包含新词、对照名词、工具书等材料；只有已核的正式审定成果按第 1 级处理 |
| 国家教育研究院乐词网 | 查学术名词、双语词汇和辞书，补充学科对应 | 正式学术名词可按第 2 级核验；其他集合按自身来源判断。保留原有地区写法与数据类别 |
| Microsoft Terminology | 查软件、系统和产品用词；优先复用官方可下载词库 | 是厂商用法。通用概念只作为第 4 级的一份证据；同一微软词库的在线页、下载包和镜像不算多个独立来源 |
| Wikidata | 复用已有第 3 级查询与采纳记录 | Q 号与中文标签仍须核实概念对应，旧结果不替代新增术语库查询 |

可依法下载的数据优先在仓库外留本地查询副本，记录下载来源、数据集或语言、可得版本、获取日期和文件哈希；在线检索保存查询式、条目链接或编号、学科、编审与公布信息、核对日期以及足以复核的定义或用例。缓存是检索工作材料，不是正式词表或来源登记；下载 TBX 作为输入不激活本库 TBX 导出草案。

精确命中与检索扩展分开记录。大小写、连字符、空格和单复数变体可以作为检索线索，保留实际命中的英文形式；复合名称不能把各组成词的中文拼接成译名。中文形式命中后仍核对定义与范围，特别区分课程领域名称、通用概念和厂商产品名。繁体词条可供判断，但地区用语变化不能由字符转换自动决定。

查询结果区分已核命中、检索完成但未命中、仅有线索、来源不可访问。接口失败、登录要求或下载失败不能写成未命中。相同来源版本、查询式和目标概念范围下复用既有结果；新增来源只补该来源，来源版本或目标范围变化时才复核受影响结果。不把旧 Wikidata 未命中扩写成所有权威来源均无。

覆盖率试查须先固定样本及选择方式，保留逐条结果，并分别报告外部中文条目命中、模型知识建议与实际采纳的比例。模型补出的名称不进入外部来源命中率，也不冒称第 4 级已核实用法。分子按主题去重，单个主题多个命中不重复计数；未完成查询另列，不据此推算来源自身的缺词率。非随机小样本只说明该样本表现，不外推全库；前批与本批的来源集合不同时，不把所有差异归因于匹配规则。

## 质量与验收

各类现行对象的完成标准如下。

| 对象 | 完成标准 |
|---|---|
| 概念文 | CONVENTIONS 的五条硬规则；权威来源链接均已核对；项目采用的 designation 有依据；索引已登记 |
| 设计文档 | 每节规则有依据或标明“本库”；已实现能力不写成待建；真实未决事项集中在文末；链接检查通过；项目采用的 designation 有依据 |
| `Application Profile` | 引用概念文、方法登记和适用决定；功能范围、模型引用、field constraints、`Usage Guidelines`、`Encoding Syntax Guidelines` 与五个操作问题完整；target binding 不改写应用无关语义 |
| 导出 artifact contract | 与语义选择交接清楚；输入快照、byte serialization、order、quoting、newlines、file set、manifest、post-generation validation 和 publication 与实现一致；确定性、完整性、原子可见性、durability 和外部符合性分别陈述 |
| 词表条目 | 现行校验脚本零问题；复制项有现行 `match` 回到源头；人工赋值字段有现行 `basis`；`self` 不为 `active` |
| 脚本 | 幂等；文本替换带断言；批量改动前后复核 Git 差异；现行阈值常量与阈值表一致；输出可读 |
| 决定记录 | 有状态与日期、背景、决定和后果；推翻时不改旧记录 |
| 文献笔记 | 标明读过和未读的部分；条款号来自原文 |
| 提交说明 | 写清改动并标注决策权级别 |

基础接口完成与正式激活完成采用不同证据。schema 生成一致、转换校验、确定性输出、索引覆盖、夹具只读、报告位置和迁移身份对账，可以验收基础能力；它们不能验收正式数据、具体来源或术语事实、委托、消费者、周期运行或切换。正式激活还需要相应人工决定、完整候选、消费者契约、应用后验证，以及风险所需的回滚证据；当前阶段没有这些证据不构成基础接口失败。

target 文件验收与消费者验收也分开。前者证明语义选择、表示和物化合同自洽；后者必须由具名消费者实际读取内容字段或检索记录并提供运行证据。生成文件可以编辑、Base 可以编辑 properties，但这些产品能力和本地修改都不构成回流、消费者启用或项目效力。

来源探测目前只能验收固定夹具行为。`--live` 禁用且输出契约未闭合，不能写成真实探测、模式通过或正式运行完成。

## 验证投入

每项审查和测试必须回答三个问题：它要发现什么失败，失败会造成什么后果，它是否提供其他门禁没有的证据。任一问题没有明确答案，或者同一风险已经由更强的 schema、哈希、写集、端到端验证或人工决定覆盖时，不设计也不执行该检查。

必须保留的检查集中在真实高风险边界。

| 风险 | 必要证据 |
|---|---|
| 语义或效力变化 | 逐项依据、决定引用和反例 |
| 决策权越界 | 授权边界与写集对账 |
| 稳定身份丢失或错误合并 | 迁移前后身份集合双向对账 |
| 数据迁移损坏 | 旧值保留、异常清单和候选差异 |
| 生成不确定 | 相同输入双跑逐字节一致 |
| 共享接口断裂 | 消费者契约和最小端到端路径 |
| 删除、正式切换或回滚失败 | 完整候选、绑定清单、应用后验证和补偿演练 |

以下检查默认没有独立价值：文件存在、固定常量回显、重复计数、包装函数透传、实现细节、相同输入的重复正例，以及已经由模式、哈希、写集或端到端检查覆盖的事实。真实风险表明其中某项具有独有价值时，计划须写明目标失败和后果后才能保留。

TDD 只用于保留下来的行为测试。文档、静态配置、确定性生成物和纯机械迁移使用直接解析、模式、哈希、差异或一次端到端验证；不为形成 RED／GREEN 步骤而另造测试。完整回归只在接口闭合、迁移闭合、完整候选、正式切换和回滚等风险边界运行。普通任务只执行定向检查。

独立审查只用于需要第二判断者的语义、权限、完整候选或正式切换风险。测试通过、格式、计数、哈希和写集等机械事实由命令证明。已发生的真实缺陷若现有门禁不能稳定复现，新增最小回归测试。

## 审计

审计追踪的层次和义务见[维护](maintenance.md)“审计追踪”。每次提交前按“验证投入”运行与写集风险直接相关的检查：Markdown 链接发生变化时运行链接检查，词表或其生成链发生变化时运行主题检查，阶段边界再运行相应完整回归；不在无关提交中重复执行。每次定期复审读取指标表；治理年审抽查决定记录是否覆盖实际发生的规则变更，并抽查 `[L1]` 提交。

来源与术语迁移账本保留历史身份、旧位置、分类、去向和阻断，是只追加审计材料。正文诊断、反向索引和固定夹具输出只保存机械证据；ignored 探测输出不进入正式审计层，也不证明真实事实。正式对象的状态与历史仍以正式数据、决定和 CHANGELOG 为准。

针对已经发生过的失败，采用以下防护。

| 失败 | 防护 |
|---|---|
| 文本替换静默失败 | 替换必须带断言；修改后用检索确认 |
| 批量转换破坏规则文本 | 规则文本中的示例使用行内代码；批量脚本跳过代码；转换后逐段复核 Git 差异 |
| 凭记忆写事实 | 写入前核对原文；核不到时标明“未核对”；引用标准在 `sources/` 中留笔记 |
| 设计跑在数据前 | 规则定稿前用真实数据试一遍 |
| 文档与数据不一致 | 现行正式数据决定当前事实；已批准未激活接口另按决定和实现核对，不用任一侧抹掉另一侧 |
| AI 越权 | 提交说明标级别；人抽查 |
| 候选识别命中被当作结论 | 保留抽取、排除、标签比较和人工判断边界；脚本输出只作复核线索 |
| 基础实现被当作正式激活 | 分开基础验收与正式激活证据；检查正式写集、决定、编辑权和消费者是否真实存在 |

## 风险

| 风险 | 控制 |
|---|---|
| AI 编造或记错事实 | 来源优先；验收要求核对；`sources/` 留笔记 |
| AI 越权改规则 | 决策权矩阵；提交说明标级别；事件后修订 |
| 未核实判断累积 | `self` 不转正；指标表 |
| 来源漂移 | 按现行 `tier` 人工复核；夹具观察不冒充真实探测 |
| 规则无用例即定稿 | 先用数据试 |
| 单人单点 | Git remote 备份；决定记录便于后续接手 |
| 治理规则本身过时 | 治理评审 |
| 部分实现提前取得效力 | 对象按正式数据、未激活基础、迁移审计和草案分层；正式激活保留人工门禁 |
| 观察或索引被当作结论 | 观察只提复核范围，索引只给影响范围；义务和工具通过不自动改正式值 |
| 探测契约被误报闭合 | 明记 `--live` 禁用和输出契约未闭合，只验收固定夹具行为 |

## 治理评审

- 政策表、决策权矩阵和阈值表每 12 个月评审一次；日期记入 `vocab/signals.yaml` 的 `governance_reviewed`，结果记决定。
- 决策权矩阵在每次越权或欠权事件后修订。
- 阈值在首轮维护，即第一次编辑审核完成后校准，之后随年审复核。
- 本文变更走政策变更流程。

## 待定事项

- 人制定的词典条目怎样复审和废弃。
- 业界惯用译名的多来源证据与第 5 级模型判断如何分别进入主题生成输入、展示与校验；现有 Wikidata 采纳结构尚不承载这些记录，本次规则扩展不批量补译既有数据。
- “结构性变更”默认指设计文档表格中的规则行，例外由人降级；首轮后评估这个判据是否充分。
- 来源和术语正式激活阶段何时开放，以及届时需要哪些完整候选、消费者和回滚证据；基础已实现不提前回答这些问题。

## 权威来源

- [ISO 37000:2021](https://www.iso.org/standard/65036.html)：治理主体与管理层的区分；正文未读，定义据 ISO 公开说明。
- [ISO 15489-1:2016](https://www.iso.org/standard/62542.html) §6：政策、职责、监控与评价，见[笔记](../sources/iso-15489.md)。
- DAMA-DMBOK 2：数据治理是规划、监控和执行的权威与控制；决策权（decision rights）。
- ANSI/NISO Z39.19-2005 §11.1.5、§11.3：编辑与编委会的审核职责，见[笔记](../sources/z39-19.md)。
