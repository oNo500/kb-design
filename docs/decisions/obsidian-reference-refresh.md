# 词表参考刷新

状态：已采纳，2026-09-05。用户要求现有 `kb-vault` 使用最新词表，并确认刷新及引用检查的影响范围。该操作不批准内容状态、来源或正式消费者激活。

## 决定

通过独立应用的显式 `refresh`，从已验证、干净且受支持的设计提交生成新参考区。只更新 vault 的 `kb/` 和 `app/manifest.json`，其他目录和文件保持原有字节。

先验证旧清单、旧 Git 输入与受管理文件，再检查新参考集合、内容受控引用及显式指向 `kb/` 的 Wikilink。受管理内容有实质修改时拒绝覆盖；Base 仅有 YAML 排版差异、且旧哈希与原生成模板一致时，可以保留实际文件并更新清单中的对应哈希，不把这种格式差异认定为用户修改了知识内容。

新文件与清单在 vault 外准备和校验，发布前检查并发修改。普通发布异常回滚，成功后保留旧参考区及清单的备份；本操作不宣称跨目录的崩溃一致性或断电持久性。

## 本次结果

本次从设计 `59e033d64b230fe658aa09955e1a66ec38aa5c6f` 更新到 `37a78c775d867eb9d1f7610ced23aee43ce345ec`。更新 776 个参考文件及清单；其余文件字节保持不变。`app/views/inbox.base` 的 YAML 语义与原生成模板一致，只保留排版差异，文件未重写。

当前内容目录为空，校验通过只证明更新与引用检查路径可运行，不构成实际内容使用证据。独立导出与新库初始化仍不覆盖非空目录。
