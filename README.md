# Obsidian 应用 (Obsidian Application)

`kb-obsidian` 是从 `kb-design` 初始化并管理 Obsidian vault 的独立应用仓库。`kb-design` 负责概念、正式词表、内容模型和治理规则；实际知识内容不进入本仓库。

## 计划命令

应用将逐步实现以下命令：

- `init`：初始化空的 Obsidian vault
- `new-content`：创建 UUIDv4 内容单元
- `validate`：校验元数据与引用
- `report`：生成使用报告

当前实现按任务逐步增加，以上命令尚未全部实现。
